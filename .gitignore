.DS_Store
/course/
.handoff
.handoff/
pi-session-*.html

# render pipeline derived artifacts (regenerated by the image pipelines)
renders/usd/
renders/out/
renders/cv-out/
scripts/__pycache__/

# npm dependency of the PDF build (KaTeX); installed by CI, never committed
node_modules/

# issue tracker is local-only (not published with the repo)
.tix/
